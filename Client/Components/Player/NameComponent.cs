namespace CryBits.Client.Components.Player;

/// <summary>Identity name stored directly on the entity — used for ECS-native player lookups.</summary>
internal struct NameComponent
{
    public string Value;
}
