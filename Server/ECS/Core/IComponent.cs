namespace CryBits.Server.ECS;

/// <summary>
/// Marker interface for all server-side ECS components.
/// Components are pure data bags — no logic, no inheritance chains.
/// </summary>
internal interface IComponent;
