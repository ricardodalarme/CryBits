namespace CryBits.Client.Components.Character;

/// <summary>
/// Marker component that identifies an entity as a non-player character (NPC).
/// Use this in queries to target only NPCs, e.g. for AI, interaction prompts or
/// distinct rendering behaviour.
/// </summary>
internal struct NpcTagComponent;
